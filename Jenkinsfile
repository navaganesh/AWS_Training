pipeline {
  agent any

stages {
    stage('build1') {
     
       steps {
        sh 'echo "FOO is $FOO and BAR is $BAR"'
             }
    }
    stage('build2') {
      steps {
        sh 'echo "FOO is $FOO and BAR is $BAR"'
      }
    }
  }
}
